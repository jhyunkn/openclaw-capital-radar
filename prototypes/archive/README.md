# Prototype Archive

Deprecated experiments and superseded visual tests belong here.

Use this folder when a visual idea is no longer active but still contains useful reference material.

Active standalone HTML prototypes should live in `prototypes/html`.
