const fs=require('fs');const path=require('path');
const root=path.join(__dirname,'..');const indexPath=path.join(root,'index.html');const statePath=path.join(root,'outputs','visual-regime-state.json');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const arr=v=>Array.isArray(v)?v:[];const n=v=>Number.isFinite(Number(v))?Number(v):0;
const cls=s=>n(s)>=70?'good':n(s)>=45?'warn':'bad';
if(!fs.existsSync(indexPath))throw new Error('index.html missing');if(!fs.existsSync(statePath))throw new Error('visual-regime-state.json missing');
const state=JSON.parse(fs.readFileSync(statePath,'utf8'));const c=state.composite||{};const p=state.permissions||{};
const metric=m=>`<article class="vis-card"><div class="ring ${cls(m.score)}" style="--p:${n(m.score)}"><b>${n(m.score)}</b></div><span>${esc(m.label)}</span><small>${esc(m.value)}</small></article>`;
const perm=(label,val)=>`<article><span>${esc(label)}</span><b>${n(val)}</b><div class="mini-meter"><i class="${cls(val)}" style="width:${n(val)}%"></i></div></article>`;
const section=`<section id="regime-section" class="panel visual-regime"><div class="section-head"><div><p class="eyebrow">Market Regime</p><h2>Visual regime board</h2></div><a class="button" href="outputs/visual-regime-state.json">Open data</a></div><div class="regime-hero"><div class="big-ring ${cls(c.score)}" style="--p:${n(c.score)}"><b>${n(c.score)}</b><span>${esc(c.label)}</span></div><div class="permission-grid">${perm('Broad add',p.broad_add)}${perm('Selective hunt',p.selective_hunt)}${perm('Trim pressure',p.trim_pressure)}${perm('Cash buffer',p.cash_buffer)}${perm('AI beta',p.ai_beta)}${perm('Crypto beta',p.crypto_beta)}</div></div><div class="visual-grid">${arr(state.inputs).map(metric).join('')}</div></section>`;
let html=fs.readFileSync(indexPath,'utf8');
const style=`<style>.visual-regime{margin-top:22px}.regime-hero{display:grid;grid-template-columns:260px 1fr;gap:16px;align-items:stretch}.big-ring,.ring{--p:0;display:grid;place-items:center;border-radius:999px;background:conic-gradient(var(--ink) calc(var(--p)*1%), rgba(251,250,246,.16) 0);position:relative}.big-ring:after,.ring:after{content:"";position:absolute;inset:10px;border-radius:999px;background:var(--paper);border:1px solid var(--rule)}.big-ring{height:240px}.big-ring b,.big-ring span,.ring b{position:relative;z-index:1}.big-ring b{font-size:72px;line-height:.9}.big-ring span{font-size:12px;text-transform:uppercase;letter-spacing:.09em;color:var(--muted)}.ring{height:94px;width:94px}.ring b{font-size:24px}.big-ring.good,.ring.good{color:var(--green)}.big-ring.warn,.ring.warn{color:var(--warn)}.big-ring.bad,.ring.bad{color:var(--red)}.permission-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));border:1px solid var(--rule);border-right:0;border-bottom:0}.permission-grid article{padding:14px;border-right:1px solid var(--rule);border-bottom:1px solid var(--rule);background:rgba(251,250,246,.14)}.permission-grid span,.vis-card span{display:block;color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.08em}.permission-grid b{display:block;font-size:30px;margin:5px 0}.mini-meter{height:8px;border:1px solid var(--rule);border-radius:999px;overflow:hidden}.mini-meter i{display:block;height:100%}.mini-meter i.good{background:var(--green)}.mini-meter i.warn{background:var(--warn)}.mini-meter i.bad{background:var(--red)}.visual-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:12px;margin-top:14px}.vis-card{display:grid;justify-items:center;gap:8px;border:1px solid var(--rule);border-radius:16px;padding:14px;background:rgba(251,250,246,.14);text-align:center}.vis-card small{font-size:11px;color:var(--muted);line-height:1.25}@media(max-width:800px){.regime-hero{grid-template-columns:1fr}.permission-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.big-ring{height:220px}}</style>`;
if(!html.includes('.visual-regime{'))html=html.replace('</head>',`${style}</head>`);
html=html.replace(/<nav class="nav">/,'<nav class="nav"><a href="#regime-section">Regime</a>');
html=html.replace(/(<\/header>)/,`$1${section}`);
html=html.replace(/<section id="brief"[\s\S]*?<section id="holdings-section"/, '<section id="holdings-section"');
fs.writeFileSync(indexPath,html);console.log('injected visual regime board and removed text-heavy brief');
