const fs = require('fs');
const path = require('path');
const root = path.join(__dirname, '..');
const indexPath = path.join(root, 'index.html');
const statePath = fs.existsSync(path.join(root, 'data', 'report-state.live.json')) ? path.join(root, 'data', 'report-state.live.json') : path.join(root, 'data', 'report-state.sample.json');
const notesDir = path.join(root, 'agent-notes', 'tickers');
const state = JSON.parse(fs.readFileSync(statePath, 'utf8'));
const holdings = Array.isArray(state.holdings) ? state.holdings : [];
const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
const list = value => Array.isArray(value) ? value : [];
function n(value){ const parsed = Number(value); return Number.isFinite(parsed) ? parsed : null; }
function fmt(value, digits=2){ const parsed = n(value); return Number.isFinite(parsed) ? parsed.toLocaleString(undefined,{maximumFractionDigits:digits}) : '—'; }
function price(value){ const parsed = n(value); return Number.isFinite(parsed) ? `$${parsed.toLocaleString(undefined,{maximumFractionDigits: parsed < 1 ? 4 : 2})}` : '—'; }
function pct(value){ const parsed = n(value); return Number.isFinite(parsed) ? `${parsed >= 0 ? '+' : ''}${parsed.toFixed(2)}%` : '—'; }
function tone(value){ const parsed = n(value); return !Number.isFinite(parsed) ? '' : parsed >= 0 ? 'good' : 'bad'; }
function signalTone(signal){ const s = String(signal || '').toUpperCase(); if(s.includes('EXIT') || s.includes('TRIM')) return 'bad'; if(s.includes('INVESTIGATE') || s.includes('WATCH')) return 'warn'; if(s.includes('ADD')) return 'blue'; return 'good'; }
function readNote(ticker){ const f = path.join(notesDir, `${String(ticker).toLowerCase()}.json`); return fs.existsSync(f) ? JSON.parse(fs.readFileSync(f,'utf8')) : null; }
function speculative(h){ return /lever|decay|speculative|tactical|crypto|option/i.test(`${h.exposureBucket || ''} ${h.role || ''} ${h.thesis || ''} ${h.ticker || ''}`); }
function valuationRelevant(h){ return !speculative(h) && !/SPY|QQQ|IWM|IBIT|BTC|ETH/i.test(String(h.ticker || '')); }
function cleanMetric(value, relevant=true){ const parsed = n(value); if(!relevant) return 'Not applicable'; if(!Number.isFinite(parsed) || parsed === 0) return 'Unavailable'; return fmt(parsed); }
function distance(current, target){ if(!(current > 0) || !(target > 0)) return null; return ((target-current)/current)*100; }
function nearest(current, values){ const nums = list(values).filter(v => typeof v === 'number' && Number.isFinite(v)); if(!nums.length || !(current > 0)) return null; return nums.map(v => ({ value:v, distancePct: distance(current,v) })).sort((a,b)=>Math.abs(a.distancePct)-Math.abs(b.distancePct))[0]; }
function actionDistances(h,note){ const current = n(h.livePrice); const map = note?.technicalMap || {}; const bands = [ ['add', nearest(current, map.buyZone), 'good'], ['support', nearest(current, map.supportLevels), 'good'], ['trim', nearest(current, map.trimZone), 'warn'], ['resistance', nearest(current, map.resistanceLevels), 'warn'], ['review', nearest(current, map.stopZone), 'bad'] ].filter(([,item])=>item && Number.isFinite(item.distancePct)); return bands.sort((a,b)=>Math.abs(a[1].distancePct)-Math.abs(b[1].distancePct)); }
function nextTrigger(h,note){ const nearestBand = actionDistances(h,note)[0]; const signal = String(h.computedSignal || h.signal || '').toUpperCase(); if(nearestBand){ const [label,item] = nearestBand; return `${label.toUpperCase()} trigger ${item.distancePct >= 0 ? '+' : ''}${item.distancePct.toFixed(2)}% away at ${price(item.value)}.`; } if(signal.includes('TRIM')) return 'Trim only if exposure remains above risk budget or underlying trend weakens.'; if(signal.includes('EXIT')) return 'Review exit conditions before adding any capital.'; if(signal.includes('INVESTIGATE')) return 'Do not act until thesis/data gap is resolved.'; return note?.strategyProtocol?.doNothingIf?.[0] || 'Do nothing unless price enters an action band or thesis changes.'; }
function strategicReason(h,note){ const signal = h.computedSignal || h.signal || 'Review'; const weight = n(h.portfolioWeightPct); const cap = speculative(h) ? 5 : 15; const nearCap = Number.isFinite(weight) && weight >= cap * .85; const dataMissing = !h.dataContract || h.dataContract?.confidence?.forwardPE === 'missing' || h.dataContract?.confidence?.fcfYield === 'missing'; const parts = []; if(nearCap) parts.push(`near ${cap}% risk cap`); else if(Number.isFinite(weight)) parts.push(`inside ${cap}% risk cap`); if(h.perf1mPct > 0 && h.perf3mPct > 0) parts.push('trend supportive'); else if(h.perf1mPct < 0 && h.perf3mPct < 0) parts.push('trend deteriorating'); else parts.push('trend mixed'); if(dataMissing) parts.push('data gap exists'); if(speculative(h)) parts.push('levered/decay risk dominates valuation'); return `${signal}: ${parts.join(' · ')}.`; }
function pressureLabel(h){ const weight = n(h.portfolioWeightPct); const cap = speculative(h) ? 5 : 15; if(!Number.isFinite(weight)) return { label:'Unknown pressure', detail:'Weight missing', cls:'warn' }; const ratio = weight / cap; if(ratio >= 1) return { label:'Over budget', detail:`${fmt(weight)}% / ${cap}% cap`, cls:'bad' }; if(ratio >= .85) return { label:'Near cap', detail:`${fmt(weight)}% / ${cap}% cap`, cls:'warn' }; return { label:'Inside budget', detail:`${fmt(weight)}% / ${cap}% cap`, cls:'good' }; }
function actionMap(h,note){ const current = n(h.livePrice); const map = note?.technicalMap || {}; const add = nearest(current,map.buyZone); const trim = nearest(current,map.trimZone); const stop = nearest(current,map.stopZone); const support = nearest(current,map.supportLevels); const resistance = nearest(current,map.resistanceLevels); const items = [ ['Add',add,'good'], ['Support',support,'good'], ['Trim',trim,'warn'], ['Review',stop,'bad'], ['Resistance',resistance,'warn'] ]; return `<div class="strategy-action-map">${items.map(([label,item,cls]) => `<span class="${cls}"><b>${esc(label)}</b><em>${item ? price(item.value) : '—'}</em></span>`).join('')}</div>`; }
function miniSpark(h,note){ const current = n(h.livePrice); const map = note?.technicalMap || {}; const stop = nearest(current,map.stopZone)?.value; const add = nearest(current,map.buyZone)?.value; const trim = nearest(current,map.trimZone)?.value; const vals = list(h.sparkline).filter(v=>typeof v==='number'&&Number.isFinite(v)); const series = vals.length > 1 ? vals : [h.perf5dPct,h.dayChangePct,h.perf1mPct].map(v => current ? current*(1+(Number(v)||0)/100) : Number(v)).filter(Number.isFinite); if(series.length < 2) return '<div class="strategy-spark empty">Chart unavailable</div>'; const all = [...series, stop, add, trim].filter(Number.isFinite); const min = Math.min(...all), max = Math.max(...all), span = max-min || 1; const y = v => 58 - ((v-min)/span)*48; const pts = series.map((v,i)=>`${(i/(series.length-1))*100},${y(v)}`).join(' '); const line = (v,cls,label) => Number.isFinite(v) ? `<line class="${cls}" x1="0" x2="100" y1="${y(v)}" y2="${y(v)}"/><text x="2" y="${Math.max(10,y(v)-2)}" class="${cls}">${label}</text>` : ''; return `<svg class="strategy-spark" viewBox="0 0 100 64" preserveAspectRatio="none">${line(add,'add','add')}${line(trim,'trim','trim')}${line(stop,'stop','review')}<polyline points="${pts}"/><circle cx="100" cy="${y(series[series.length-1])}" r="1.8"/></svg>`; }
function card(h){ const note = readNote(h.ticker); const sig = h.computedSignal || h.signal || 'Review'; const pressure = pressureLabel(h); const relevantValuation = valuationRelevant(h); const fpe = h.dataContract?.forwardPE ?? h.forwardPE ?? h.finviz?.metrics?.['Forward P/E']; const fcf = h.dataContract?.fcfYield ?? h.fcfYield; const nearestBand = actionDistances(h,note)[0]; return `<article class="strategy-card ${signalTone(sig)}"><div class="strategy-card-top"><div><span class="strategy-role">${esc(h.exposureBucket || h.role || 'Holding')}</span><h3>${esc(h.ticker)}</h3></div><div class="strategy-price"><b>${price(h.livePrice)}</b><span class="${tone(h.dayChangePct)}">${pct(h.dayChangePct)}</span></div></div><div class="strategy-signal-row"><strong>${esc(sig)}</strong><em class="${pressure.cls}">${esc(pressure.label)}</em></div><p class="strategy-reason">${esc(strategicReason(h,note))}</p>${miniSpark(h,note)}${actionMap(h,note)}<div class="strategy-grid"><div><span>Next trigger</span><b>${esc(nextTrigger(h,note))}</b></div><div><span>Distance to action</span><b>${nearestBand ? `${nearestBand[1].distancePct >= 0 ? '+' : ''}${nearestBand[1].distancePct.toFixed(2)}% to ${nearestBand[0]}` : 'No band mapped'}</b></div><div><span>Position pressure</span><b>${esc(pressure.detail)}</b></div><div><span>Momentum</span><b><span class="${tone(h.perf5dPct)}">${pct(h.perf5dPct)}</span> / <span class="${tone(h.perf1mPct)}">${pct(h.perf1mPct)}</span></b></div><div><span>Valuation read</span><b>${relevantValuation ? `PE ${cleanMetric(fpe)} · FCF ${cleanMetric(fcf)}${cleanMetric(fcf) === 'Unavailable' ? '' : '%'}` : 'Not applicable to this exposure'}</b></div><div><span>Data confidence</span><b>${esc(h.dataContract?.confidence?.forwardPE || 'missing')} / ${esc(h.dataContract?.confidence?.nextEarningsDate || 'missing')}</b></div></div><p class="strategy-next">${esc(note?.strategyProtocol?.doNothingIf?.[0] || h.watch || 'Wait for a mapped trigger before reacting.')}</p><a class="detail-link" href="pages/${esc(String(h.ticker).toLowerCase())}.html">Open strategy workbench →</a></article>`; }
const css = `<style>.strategy-holdings{background:rgba(251,250,246,.1)}.strategy-intro{max-width:920px;margin:-14px 0 26px;color:var(--muted);font-size:16px}.strategy-card-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(330px,1fr));gap:0;border-top:1px solid var(--rule);border-left:1px solid var(--rule)}.strategy-card{border-right:1px solid var(--rule);border-bottom:1px solid var(--rule);background:rgba(251,250,246,.18);padding:20px;min-height:620px}.strategy-card-top{display:flex;justify-content:space-between;gap:16px}.strategy-role{display:block;color:var(--muted);font-size:13px;line-height:1.25}.strategy-card h3{font-size:44px;line-height:.9;letter-spacing:-.06em;font-weight:500;margin:8px 0 0}.strategy-price{text-align:right}.strategy-price b{display:block;font-size:28px;line-height:1;font-weight:500;letter-spacing:-.04em}.strategy-price span{font-size:13px}.strategy-signal-row{display:flex;justify-content:space-between;align-items:center;gap:12px;margin:22px 0 12px}.strategy-signal-row strong{font-size:20px;line-height:1;border:1px solid var(--rule);border-radius:999px;padding:9px 12px;background:rgba(251,250,246,.44)}.strategy-signal-row em{font-style:normal;font-size:13px}.strategy-card.bad .strategy-signal-row strong{color:var(--red);border-color:rgba(159,63,53,.32)}.strategy-card.warn .strategy-signal-row strong{color:var(--warn);border-color:rgba(138,106,44,.32)}.strategy-card.good .strategy-signal-row strong{color:var(--green);border-color:rgba(47,111,78,.32)}.strategy-reason{font-size:16px;line-height:1.4;min-height:68px}.strategy-spark{width:100%;height:96px;margin:18px 0 10px;background:rgba(251,250,246,.28);border:1px solid var(--rule2)}.strategy-spark polyline{fill:none;stroke:#d7a84c;stroke-width:2;vector-effect:non-scaling-stroke}.strategy-spark circle{fill:#24231f}.strategy-spark line{stroke-width:.8;stroke-dasharray:2 2;vector-effect:non-scaling-stroke}.strategy-spark text{font-size:5px;letter-spacing:.02em}.strategy-spark .add{stroke:var(--green);fill:var(--green)}.strategy-spark .trim{stroke:var(--warn);fill:var(--warn)}.strategy-spark .stop{stroke:var(--red);fill:var(--red)}.strategy-action-map{display:grid;grid-template-columns:repeat(5,1fr);gap:0;border-top:1px solid var(--rule2);border-left:1px solid var(--rule2);margin-bottom:16px}.strategy-action-map span{border-right:1px solid var(--rule2);border-bottom:1px solid var(--rule2);padding:8px 6px}.strategy-action-map b{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.06em;color:var(--muted);font-weight:500}.strategy-action-map em{display:block;font-style:normal;font-size:13px;margin-top:5px}.strategy-action-map .good em{color:var(--green)}.strategy-action-map .warn em{color:var(--warn)}.strategy-action-map .bad em{color:var(--red)}.strategy-grid{display:grid;gap:0;border-top:1px solid var(--rule2)}.strategy-grid div{display:flex;justify-content:space-between;gap:12px;border-bottom:1px solid var(--rule2);padding:9px 0}.strategy-grid span{color:var(--muted);font-size:13px}.strategy-grid b{text-align:right;font-size:13px;max-width:62%;font-weight:600}.strategy-next{margin-top:14px;color:var(--muted);font-size:14px}.strategy-card .detail-link{margin-top:18px}.holdings-grid.is-replaced{display:none!important}@media(max-width:760px){.strategy-card{min-height:auto}.strategy-card h3{font-size:36px}.strategy-action-map{grid-template-columns:1fr 1fr}.strategy-grid div{display:block}.strategy-grid b{display:block;text-align:left;max-width:none;margin-top:4px}}</style>`;
const html = `<section id="holdings-section" class="panel strategy-holdings"><div class="section-head"><div><p class="eyebrow">Strategic Holdings</p><h2>Decision cards, not data cards</h2></div></div><p class="strategy-intro">Each holding is organized around the decision: current posture, reason, next trigger, distance to action, risk-budget pressure, valuation relevance, and data confidence. Raw facts are kept only when they support a strategic reaction.</p><div class="strategy-card-grid">${holdings.map(card).join('')}</div></section>`;
if(!fs.existsSync(indexPath)) throw new Error('index.html missing');
let index = fs.readFileSync(indexPath,'utf8');
index = index.replace(/<style>\.strategy-holdings[\s\S]*?<\/style>/,'');
index = index.replace(/<section id="holdings-section" class="panel strategy-holdings"[\s\S]*?<section id="opportunities-section"/, '<section id="opportunities-section"');
index = index.replace('</head>', `${css}</head>`);
if(index.includes('<section id="holdings-section"')){
  index = index.replace(/<section id="holdings-section"[\s\S]*?<section id="opportunities-section"/, `${html}<section id="opportunities-section"`);
} else {
  index = index.replace('<section id="opportunities-section"', `${html}<section id="opportunities-section"`);
}
fs.writeFileSync(indexPath,index);
console.log(`injected ${holdings.length} strategic holding decision cards`);
