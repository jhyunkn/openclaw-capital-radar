const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const arr=v=>Array.isArray(v)?v:[];
const num=v=>Number.isFinite(Number(v))?Number(v):null;
function take(rows,years=10){const v=arr(rows).filter(r=>r&&r.date&&Number.isFinite(Number(r.value))).map(r=>({date:r.date,value:Number(r.value)}));if(!v.length)return[];const end=v[v.length-1];const cut=new Date(`${end.date}T00:00:00Z`);cut.setUTCFullYear(cut.getUTCFullYear()-years);return v.filter(r=>new Date(`${r.date}T00:00:00Z`)>=cut);}
function domain(series){const all=Object.values(series||{}).flatMap(arr);const vals=all.map(r=>Number(r.value)).filter(Number.isFinite);const dates=all.map(r=>new Date(`${r.date}T00:00:00Z`).getTime()).filter(Number.isFinite);if(!vals.length||!dates.length)return null;const min=Math.min(...vals),max=Math.max(...vals),pad=Math.max((max-min)*.12,8);return{minDate:Math.min(...dates),maxDate:Math.max(...dates),minValue:min-pad,maxValue:max+pad};}
function pathRows(rows,d,w,h,p){if(!d||!rows.length)return'';const iw=w-p.left-p.right,ih=h-p.top-p.bottom;return rows.map((r,i)=>{const t=new Date(`${r.date}T00:00:00Z`).getTime();const x=p.left+((t-d.minDate)/Math.max(1,d.maxDate-d.minDate))*iw;const y=p.top+(1-((r.value-d.minValue)/Math.max(1e-9,d.maxValue-d.minValue)))*ih;return`${i?'L':'M'}${x.toFixed(1)},${y.toFixed(1)}`;}).join(' ');}
function fmt(v,d=1){const n=num(v);return n==null?'—':n.toFixed(d);}
function renderChart(state){const raw=state?.chart_series?.commodities_stack?.series||{};const series={oil:take(raw.oil,10),natural_gas:take(raw.natural_gas,10),uranium:take(raw.uranium,10),copper:take(raw.copper,10),agriculture_basket:take(raw.agriculture_basket,10),silver:take(raw.silver,10)};const d=domain(series);if(!d)return`<div class="cmdty-empty">Commodities chart data pending.</div>`;const w=920,h=320,p={left:42,right:26,top:24,bottom:34};const lens=state?.diagnostic_lenses||{};return`<div class="cmdty-chart-shell"><div class="cmdty-chart-topline"><div><span>Raw chart</span><b>Commodities: energy, industrial, food, monetary metal</b></div><div class="cmdty-current-strip"><article><span>Energy</span><b>${esc(fmt(lens.energy?.score))}</b></article><article><span>Industrial</span><b>${esc(fmt(lens.industrial?.score))}</b></article><article><span>Physical</span><b>${esc(fmt(state?.derived?.physical_constraint_score?.value))}</b></article></div></div><svg class="cmdty-svg" viewBox="0 0 ${w} ${h}" role="img" aria-label="Commodities chart"><line x1="${p.left}" y1="${h-p.bottom}" x2="${w-p.right}" y2="${h-p.bottom}" class="cmdty-axis"/><line x1="${p.left}" y1="${p.top}" x2="${p.left}" y2="${h-p.bottom}" class="cmdty-axis"/><path d="${esc(pathRows(series.oil,d,w,h,p))}" class="cmdty-line oil"/><path d="${esc(pathRows(series.natural_gas,d,w,h,p))}" class="cmdty-line gas"/><path d="${esc(pathRows(series.uranium,d,w,h,p))}" class="cmdty-line uranium"/><path d="${esc(pathRows(series.copper,d,w,h,p))}" class="cmdty-line copper"/><path d="${esc(pathRows(series.agriculture_basket,d,w,h,p))}" class="cmdty-line agri"/><path d="${esc(pathRows(series.silver,d,w,h,p))}" class="cmdty-line silver"/><text x="${p.left}" y="${h-9}" class="cmdty-axis-label">${esc(series.oil[0]?.date||'')}</text><text x="${w-p.right-82}" y="${h-9}" class="cmdty-axis-label">${esc(series.oil.at(-1)?.date||'')}</text></svg><div class="cmdty-legend"><span class="oil">Oil</span><span class="gas">Natural gas</span><span class="uranium">Uranium</span><span class="copper">Copper</span><span class="agri">Agriculture</span><span class="silver">Silver</span></div></div>`;}
function lensCards(state){const lens=state?.diagnostic_lenses||{};return ['energy','industrial','food','monetary_metal'].map(k=>{const l=lens[k]||{};return`<article><span>${esc(k.replace('_',' '))}</span><b>${esc(fmt(l.score))}</b><small>${esc(l.question||'Pending lens question.')}</small></article>`;}).join('');}
function renderCommoditiesWorkbench(state){const missing=arr(state?.missing_evidence);return`<section class="macro-operating-block commodities-workbench"><div class="macro-block-title"><p class="eyebrow">Asset-class workbench</p><h3>Commodities: physical scarcity and buildout pressure.</h3></div><div class="cmdty-grid"><div>${renderChart(state)}<div class="cmdty-lens-grid">${lensCards(state)}</div><div class="cmdty-behavior-card"><span>Capital behavior</span><b>${esc(state?.capital_behavior?.behavior||'Physical scarcity')}</b><small>${esc(state?.capital_behavior?.strategy_posture||'Use commodities to detect scarcity and industrial buildout pressure.')}</small></div></div><aside class="cmdty-sidecar"><article><span>Coverage</span><b>${esc(state?.coverage||'PENDING')}</b><small>Six-dataset frozen scope. Seed-backed first pass.</small></article><article><span>Missing evidence</span><b>${esc(missing.length)}</b><small>${esc(missing.join(', '))}</small></article><article><span>Current read</span><b>${esc(state?.web_summary?.current_read||'Pending')}</b><small>${esc(state?.analysis?.statement||'Chart first; text secondary.')}</small></article></aside></div></section>`;}
function renderCommoditiesWorkbenchStyle(){return`<style>.commodities-workbench{--cmd-oil:#a4502f;--cmd-gas:#4d6f91;--cmd-uranium:#6f8b4d;--cmd-copper:#b87333;--cmd-agri:#8b7e6e;--cmd-silver:#6e5aa8}.cmdty-grid{display:grid;grid-template-columns:minmax(0,1fr) 250px;gap:10px}.cmdty-chart-shell,.cmdty-sidecar article,.cmdty-behavior-card,.cmdty-lens-grid article{border:1px solid var(--rule);border-radius:20px;background:#ffffff;padding:14px}.cmdty-chart-topline{display:flex;justify-content:space-between;gap:18px;margin-bottom:8px}.cmdty-chart-topline span,.cmdty-current-strip span,.cmdty-sidecar span,.cmdty-behavior-card span,.cmdty-lens-grid span{display:block;color:var(--muted);font-size:9px;text-transform:uppercase;letter-spacing:.1em}.cmdty-chart-topline b{display:block;font-size:22px;line-height:1.05;letter-spacing:-.035em;font-weight:500;margin-top:4px}.cmdty-current-strip{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px;min-width:280px}.cmdty-current-strip article{border:1px solid var(--rule);border-radius:14px;padding:8px;background:rgba(255,255,255,.18)}.cmdty-current-strip b,.cmdty-sidecar b,.cmdty-behavior-card b,.cmdty-lens-grid b{display:block;font-size:17px;font-weight:500;margin-top:4px}.cmdty-svg{width:100%;height:auto;display:block}.cmdty-axis{stroke:rgba(36,35,31,.28);stroke-width:1}.cmdty-line{fill:none;stroke-width:2.1;vector-effect:non-scaling-stroke}.cmdty-line.oil{stroke:var(--cmd-oil);stroke-width:2.8}.cmdty-line.gas{stroke:var(--cmd-gas)}.cmdty-line.uranium{stroke:var(--cmd-uranium)}.cmdty-line.copper{stroke:var(--cmd-copper);stroke-width:2.6}.cmdty-line.agri{stroke:var(--cmd-agri)}.cmdty-line.silver{stroke:var(--cmd-silver)}.cmdty-axis-label{fill:rgba(36,35,31,.62);font-size:10px}.cmdty-legend{display:flex;gap:12px;flex-wrap:wrap;color:var(--muted);font-size:11px}.cmdty-legend span:before{content:'';display:inline-block;width:16px;height:2px;margin-right:5px;vertical-align:middle;background:currentColor}.cmdty-legend .oil{color:var(--cmd-oil)}.cmdty-legend .gas{color:var(--cmd-gas)}.cmdty-legend .uranium{color:var(--cmd-uranium)}.cmdty-legend .copper{color:var(--cmd-copper)}.cmdty-legend .agri{color:var(--cmd-agri)}.cmdty-legend .silver{color:var(--cmd-silver)}.cmdty-sidecar{display:grid;gap:8px;align-content:start}.cmdty-sidecar small,.cmdty-behavior-card small,.cmdty-lens-grid small{display:block;color:var(--muted);font-size:11px;line-height:1.35;margin-top:6px}.cmdty-lens-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:10px}.cmdty-behavior-card{margin-top:10px}.cmdty-empty{border:1px dashed var(--rule);border-radius:16px;padding:18px;color:var(--muted);font-size:12px}@media(max-width:980px){.cmdty-grid{grid-template-columns:1fr}.cmdty-current-strip{min-width:0}.cmdty-lens-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:620px){.cmdty-chart-topline{display:block}.cmdty-current-strip,.cmdty-lens-grid{grid-template-columns:1fr;margin-top:10px}}</style>`;}
module.exports={renderCommoditiesWorkbench,renderCommoditiesWorkbenchStyle};
