const fs = require('fs');
const path = require('path');
const root = path.join(__dirname, '..');
const statePath = fs.existsSync(path.join(root, 'data', 'report-state.live.json')) ? path.join(root, 'data', 'report-state.live.json') : path.join(root, 'data', 'report-state.sample.json');
const state = JSON.parse(fs.readFileSync(statePath, 'utf8'));
const { strategyFor, numericLevels } = require('./capital-radar-strategy-rules.cjs');
const outDir = path.join(root, 'pages'); fs.mkdirSync(outDir, { recursive: true });
const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt = n => typeof n === 'number' ? n.toLocaleString(undefined, { maximumFractionDigits: n < 1 ? 4 : 2 }) : 'n/a';
const price = n => n == null ? 'n/a' : `$${Number(n).toLocaleString(undefined,{maximumFractionDigits:n < 1 ? 4 : 2})}`;
const pct = n => typeof n === 'number' ? `${n >= 0 ? '+' : ''}${n.toFixed(2)}%` : 'n/a';
const money = n => typeof n === 'number' ? `${n >= 0 ? '+' : '-'}$${Math.abs(n).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}` : 'n/a';
const tone = n => typeof n !== 'number' ? '' : n >= 0 ? 'good' : 'bad';
function tvSymbol(ticker){
  const t = String(ticker || '').toUpperCase();
  const map = {
    'SPY':'AMEX:SPY','QQQ':'NASDAQ:QQQ','IWM':'AMEX:IWM','IBIT':'NASDAQ:IBIT',
    'MSFT':'NASDAQ:MSFT','AMZN':'NASDAQ:AMZN','META':'NASDAQ:META','NVDA':'NASDAQ:NVDA','GOOGL':'NASDAQ:GOOGL','NFLX':'NASDAQ:NFLX',
    'GEV':'NYSE:GEV','CEG':'NASDAQ:CEG','MA':'NYSE:MA','AVGO':'NASDAQ:AVGO','VRT':'NYSE:VRT',
    'CONL':'NASDAQ:CONL','TSLT':'NASDAQ:TSLT','TSNF':'NASDAQ:TSNF','BMNR':'AMEX:BMNR',
    'BTC-USD':'BITSTAMP:BTCUSD','ETH-USD':'BITSTAMP:ETHUSD','^VIX':'CBOE:VIX'
  };
  return map[t] || `NASDAQ:${t}`;
}
function tradingViewChart(h){
  const symbol = tvSymbol(h.ticker);
  const containerId = `tradingview_${String(h.ticker).toLowerCase().replace(/[^a-z0-9]/g,'_')}`;
  const fallback = h.chart?.path ? `<details class="fallback-chart"><summary>Open static external chart snapshot</summary><a href="${esc(h.finviz?.quoteUrl || '#')}" target="_blank" rel="noreferrer"><img class="realchart" src="/${esc(h.chart.path)}" alt="${esc(h.ticker)} external market chart snapshot"></a></details>` : '<p class="label">No static external chart snapshot available.</p>';
  return `<div class="tv-wrap"><div id="${containerId}" class="tvchart"></div></div><script src="https://s3.tradingview.com/tv.js"></script><script>(function(){function mount(){if(!window.TradingView)return setTimeout(mount,200);new TradingView.widget({"autosize":true,"symbol":"${esc(symbol)}","interval":"D","timezone":"America/New_York","theme":"dark","style":"1","locale":"en","enable_publishing":false,"allow_symbol_change":true,"withdateranges":true,"hide_side_toolbar":false,"details":true,"hotlist":false,"calendar":false,"studies":["Volume@tv-basicstudies","MASimple@tv-basicstudies","RSI@tv-basicstudies","MACD@tv-basicstudies"],"container_id":"${containerId}"});}mount();})();</script>${fallback}`;
}
function compactNum(n){ return typeof n === 'number' ? n.toLocaleString(undefined,{maximumFractionDigits:n>1000?0:2}) : 'n/a'; }
function flowPanel(h){ const p=h.finviz?.parsed||{}, im=h.finviz?.implication||{}; return `<section class="section"><h2>Institutional / flow read</h2><div class="prices"><article class="pricecard"><span>Relative volume</span><b>${compactNum(p.relVolume)}x</b></article><article class="pricecard"><span>Volume / avg</span><b>${compactNum(p.volume)} / ${compactNum(p.avgVolume)}</b></article><article class="pricecard"><span>RSI</span><b>${compactNum(p.rsi)}</b></article><article class="pricecard"><span>Inst own</span><b>${compactNum(p.instOwn)}%</b></article><article class="pricecard"><span>Short float</span><b>${compactNum(p.shortFloat)}%</b></article><article class="pricecard"><span>ATR</span><b>${price(p.atr)}</b></article></div><p><b>${esc(im.flow||'Flow unavailable')}</b> · ${esc(im.trend||'Trend unavailable')} · ${esc(im.institutional||'Institutional data unavailable')} · ${esc(im.shortInterest||'Short-interest unavailable')}</p></section>`; }
const css = `:root{--bg:#050505;--ink:#f4f1ea;--muted:#a7a29a;--line:rgba(244,241,234,.15);--panel:rgba(244,241,234,.055);--panel2:rgba(244,241,234,.095);--good:#62d78e;--warn:#d9a84d;--bad:#ff756d;--blue:#92c7ff}*{box-sizing:border-box}body{margin:0;background:#050505;color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Arial,sans-serif}body:before{content:"";position:fixed;inset:0;pointer-events:none;background:radial-gradient(circle at 80% 5%,rgba(216,168,74,.18),transparent 28%),radial-gradient(circle at 8% 25%,rgba(146,199,255,.10),transparent 24%),linear-gradient(180deg,#0c0c0b 0%,#050505 42%,#080806 100%);z-index:-2}.page{max-width:1440px;margin:auto;padding:18px clamp(18px,3.5vw,56px) 80px}.top{position:sticky;top:0;z-index:20;display:flex;justify-content:space-between;gap:16px;padding:14px 0;background:rgba(5,5,5,.76);backdrop-filter:blur(22px);border-bottom:1px solid var(--line);font-size:13px}.top a{color:var(--ink);text-decoration:none}.hero{min-height:48vh;display:grid;grid-template-columns:1fr 420px;gap:48px;align-items:center;padding:54px 0;border-bottom:1px solid var(--line)}.kicker,.label{font-size:12px;color:var(--muted);text-transform:uppercase;letter-spacing:.12em;font-weight:850}h1{font-size:clamp(72px,12vw,180px);line-height:.82;letter-spacing:-.09em;margin:0;font-weight:560}h2{font-size:clamp(36px,5vw,76px);line-height:.92;letter-spacing:-.065em;margin:0 0 18px;font-weight:560}p,li{color:#d5d0c8;line-height:1.5}.stamp{border:1px solid var(--line);border-radius:32px;background:linear-gradient(180deg,var(--panel2),var(--panel));padding:24px}.stamp strong{display:block;font-size:58px;letter-spacing:-.06em}.grid{display:grid;gap:14px}.metrics{grid-template-columns:repeat(4,minmax(0,1fr));padding:30px 0}.metric,.panel,.pricecard{border:1px solid var(--line);border-radius:26px;background:linear-gradient(180deg,var(--panel2),var(--panel));padding:18px}.metric span,.pricecard span{display:block;color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.12em;font-weight:850}.metric strong{display:block;font-size:28px;margin-top:8px;letter-spacing:-.04em}.good{color:var(--good)!important}.bad{color:var(--bad)!important}.warn{color:var(--warn)!important}.section{padding:42px 0;border-bottom:1px solid var(--line)}.tv-wrap{height:680px;border-radius:28px;overflow:hidden;border:1px solid var(--line);background:#0b0b0a}.tvchart{height:100%;width:100%}.realchart{display:block;width:100%;max-height:560px;object-fit:contain;border-radius:20px;border:1px solid var(--line);background:#0b0b0a;filter:saturate(.96) contrast(1.02);margin-top:14px}.fallback-chart{margin-top:14px;color:var(--muted)}.fallback-chart summary{cursor:pointer;color:var(--ink);font-size:13px}.chart.empty{display:grid;place-items:center;border:1px dashed var(--line);border-radius:22px;color:var(--muted);height:320px}.prices{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px}.pricecard b{display:block;font-size:24px;margin-top:8px;letter-spacing:-.05em}.split{display:grid;grid-template-columns:1fr 1fr;gap:14px}.smallgrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.force{border-top:1px solid var(--line);padding-top:12px;margin-top:12px}.footer{color:var(--muted);font-size:12px;padding-top:20px}.notice{border:1px solid var(--line);border-radius:24px;padding:16px;background:rgba(216,168,74,.08);margin-top:18px}@media(max-width:1000px){.hero,.split{grid-template-columns:1fr}.metrics,.prices{grid-template-columns:1fr 1fr}.tv-wrap{height:560px}}@media(max-width:620px){.page{padding-inline:14px}.metrics,.prices,.smallgrid{grid-template-columns:1fr}.tv-wrap{height:460px}}`;
function pageFor(h){
  const rule = strategyFor(h.ticker), levels = numericLevels(h.ticker, h.livePrice, h.finviz?.parsed);
  const affected = (state.strategy?.marketForces || []).filter(f => (f.affected || []).includes(h.ticker));
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${esc(h.ticker)} · Rating + Live Chart Workspace</title><style>${css}</style></head><body><main class="page"><div class="top"><a href="/outputs/capital-radar-current.html">← Capital Radar</a><span>${esc(state.meta?.reportDate || '')} · rating + live market chart workspace</span></div><header class="hero"><section><p class="kicker">Ticker workspace · rating + live chart together</p><h1>${esc(h.ticker)}</h1><p>${esc(h.role || h.exposureBucket || 'Holding')} · ${esc(rule.posture)}. This page combines the rating, real market-data chart, numeric action bands, flow read, thesis, and evidence in one place.</p><div class="notice"><b>Chart integrity rule:</b> the primary chart below is a TradingView real market-data widget. Synthetic candles are not used. The static Finviz image, when available, is only a secondary snapshot.</div></section><aside class="stamp"><span class="label">Current price</span><strong>${price(h.livePrice)}</strong><p>Signal: ${esc(h.computedSignal || h.signal || '')}<br>Total return: <b class="${tone(h.totalReturnUsd)}">${money(h.totalReturnUsd)} / ${pct(h.totalReturnPct)}</b></p></aside></header><section class="grid metrics"><article class="metric"><span>Value</span><strong>$${fmt(h.marketValue)}</strong></article><article class="metric"><span>Weight</span><strong>${fmt(h.portfolioWeightPct)}%</strong></article><article class="metric"><span>Day / 1M</span><strong><span class="${tone(h.dayChangePct)}">${pct(h.dayChangePct)}</span> / <span class="${tone(h.perf1mPct)}">${pct(h.perf1mPct)}</span></strong></article><article class="metric"><span>Horizon</span><strong>${esc(levels.horizon)}</strong></article></section><section class="section"><h2>Live market chart</h2>${tradingViewChart(h)}<p class="label">Primary chart source: TradingView market-data widget for ${esc(tvSymbol(h.ticker))}. Use with numeric bands below; this is not a synthetic fallback.</p></section>${flowPanel(h)}<section class="section"><h2>Numbers to act around</h2><div class="prices"><article class="pricecard"><span>Buy zone</span><b class="good">${price(levels.entryLow)}–${price(levels.entryHigh)}</b></article><article class="pricecard"><span>Add below</span><b class="good">${price(levels.addBelow)}</b></article><article class="pricecard"><span>Sell / trim</span><b class="warn">${price(levels.trimLow)}–${price(levels.trimHigh)}</b></article><article class="pricecard"><span>Stop / review</span><b class="bad">${price(levels.stop)}</b></article><article class="pricecard"><span>Hard exit</span><b class="bad">${price(levels.hardExit)}</b></article><article class="pricecard"><span>Target</span><b>${price(levels.target)}</b></article></div><p>Basis: ${esc(levels.basis)}. These are operating bands, not permanent truths; update as market structure changes.</p></section><section class="split section"><article class="panel"><h2>Thesis</h2><p>${esc(h.thesis || h.actionRationale || 'No thesis loaded.')}</p><p><b>Watch:</b> ${esc(h.watch || 'No watch item loaded.')}</p></article><article class="panel"><h2>Rule text</h2><div class="smallgrid"><p><b>Hold:</b> ${esc(rule.holdUntil)}</p><p><b>Add:</b> ${esc(rule.addWhen)}</p><p><b>Trim:</b> ${esc(rule.trimWhen)}</p><p><b>Exit:</b> ${esc(rule.exitWhen)}</p></div></article></section><section class="section"><h2>Forces / evidence</h2>${affected.map(f=>`<div class="force"><b>${esc(f.name)} · ${esc(f.direction)}</b><p>${esc(f.interpretation)}</p><p class="label">${esc((f.evidence||[]).join(' · '))}</p></div>`).join('') || '<p>No active market force mapped.</p>'}</section><footer class="footer">Generated from ${esc(path.relative(root,statePath))}. Research support only; no automatic brokerage action.</footer></main></body></html>`;
}
for (const h of state.holdings || []) fs.writeFileSync(path.join(outDir, `${String(h.ticker).toLowerCase()}.html`), pageFor(h));
console.log(`generated ${state.holdings?.length || 0} combined rating + live chart ticker workspaces`);
