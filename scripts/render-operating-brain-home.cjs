const fs = require('fs');
const path = require('path');

const root = path.join(__dirname, '..');
const out = path.join(root, 'outputs');
const indexPath = path.join(root, 'index.html');

function readJson(name, fallback) { try { return JSON.parse(fs.readFileSync(path.join(out, name), 'utf8')); } catch (_) { return fallback; } }
function esc(value) { return String(value ?? '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c])); }
function fmt(value, digits = 2) { const n = Number(value); return Number.isFinite(n) ? n.toLocaleString(undefined, { maximumFractionDigits: digits }) : esc(value ?? '—'); }
function cls(value) { const s = String(value || '').toUpperCase(); if (s.includes('EXIT') || s.includes('TRIM') || s.includes('BLOCK')) return 'bad'; if (s.includes('NO_ADD') || s.includes('VERIFY') || s.includes('WATCH') || s.includes('RESEARCH')) return 'warn'; if (s.includes('ADD') || s.includes('PROMOTED')) return 'good'; return ''; }
function list(value) { return Array.isArray(value) ? value : []; }
function hasEvidenceIds(item) { return Array.isArray(item?.evidence_ids) && item.evidence_ids.length > 0; }

const regime = readJson('market-regime-state.json', {});
const portfolio = readJson('portfolio-decision-state.json', []);
const opportunities = readJson('opportunity-promotion-state.json', []);
const truth = readJson('data-truth-state.json', {});
const cadence = readJson('operating-cadence-state.json', {});
const landscape = readJson('market-landscape-state.json', null);
const evidenceMap = readJson('institutional-evidence-map.json', { evidence: [] });
const evidenceById = Object.fromEntries(list(evidenceMap.evidence).map(item => [item.id, item]));
const landscapeRenderable = Boolean(landscape && landscape.render_permission === true && list(landscape.market_focus).length && list(landscape.market_worries).length && hasEvidenceIds(landscape.global_finance_shift) && hasEvidenceIds(landscape.directional_thesis));
const health = truth.homepageSafeToRender && landscapeRenderable ? 'fresh' : 'stale';
const trustText = health === 'fresh' ? 'Artifact render allowed' : 'Render blocked by data truth or evidence validator';
const landscapeThesis = landscapeRenderable ? landscape.directional_thesis.base_case : 'Evidence-backed market landscape unavailable. Brief is degraded until required evidence artifacts validate.';

const style = `<style>
.trust-strip{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:0;border:1px solid var(--rule);border-right:0;border-bottom:0;margin-top:22px}.trust-strip article{padding:14px;border-right:1px solid var(--rule);border-bottom:1px solid var(--rule);background:rgba(251,250,246,.16)}.trust-strip span,.artifact-list span,.landscape-card span,.translation-strip span,.evidence-drawer span,.macro-group span{display:block;color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.08em}.trust-strip b{display:block;margin-top:8px;font-size:18px;line-height:1.05}.artifact-card{border:1px solid var(--rule);padding:16px;background:rgba(251,250,246,.16);min-height:220px}.artifact-card h3{font-size:30px;line-height:.95;letter-spacing:-.05em;margin:8px 0}.artifact-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:14px}.artifact-list{display:grid;gap:8px;margin-top:12px}.artifact-list div{display:grid;grid-template-columns:.85fr 1.15fr;gap:10px;border-top:1px solid var(--rule2);padding-top:8px}.pill{display:inline-flex;border:1px solid var(--rule);border-radius:999px;padding:7px 10px;font-size:12px;text-transform:uppercase;letter-spacing:.04em}.pill.good{color:var(--green);border-color:rgba(47,111,78,.32)}.pill.warn{color:var(--warn);border-color:rgba(138,106,44,.32)}.pill.bad{color:var(--red);border-color:rgba(159,63,53,.32)}.landscape-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin-top:22px}.landscape-card{border:1px solid var(--rule);border-radius:16px;padding:15px;background:rgba(251,250,246,.18)}.landscape-card b{display:block;margin-top:9px;font-size:15px;line-height:1.32}.translation-strip{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}.translation-strip article,.macro-group{border:1px solid var(--rule);border-radius:16px;padding:15px;background:rgba(251,250,246,.14)}.translation-strip b{display:block;margin-top:9px;font-size:15px;line-height:1.32}.evidence-drawer{margin-top:18px;border:1px solid var(--rule);border-radius:18px;overflow:hidden;background:rgba(251,250,246,.12)}.evidence-drawer summary{cursor:pointer;padding:15px 16px;font-size:13px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted)}.evidence-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));border-top:1px solid var(--rule);border-left:1px solid var(--rule)}.evidence-grid article{padding:14px;border-right:1px solid var(--rule);border-bottom:1px solid var(--rule)}.evidence-grid b{display:block;margin-top:8px;font-size:15px;line-height:1.35}.evidence-grid small{display:block;margin-top:8px;color:var(--muted);line-height:1.35}.degraded{border:1px solid rgba(159,63,53,.35);background:rgba(159,63,53,.08);border-radius:18px;padding:18px;margin-top:18px}.macro-group{margin-bottom:14px}.macro-group h3{font-size:24px;letter-spacing:-.04em;margin:8px 0 10px}@media(max-width:1000px){.landscape-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:720px){.artifact-list div,.landscape-grid,.translation-strip{grid-template-columns:1fr}}
</style>`;

function evidenceCards(ids) {
  const rows = list(ids).map(id => evidenceById[id]).filter(Boolean);
  if (!rows.length) return '<p class="muted">No resolved evidence records.</p>';
  return `<div class="evidence-grid">${rows.map(ev => `<article><span>${esc(ev.claim_type)} · confidence ${fmt(ev.confidence, 2)}</span><b>${esc(ev.source_name)}</b><small>${esc(ev.extracted_insight)}</small><small>Affects: ${esc(list(ev.affected_thesis).join(' · ') || 'unmapped')}</small><small>${esc(ev.url || ev.citation || 'citation pending')}</small></article>`).join('')}</div>`;
}
function allLandscapeEvidenceIds() {
  const ids = new Set();
  list(landscape?.market_focus).forEach(item => list(item.evidence_ids).forEach(id => ids.add(id)));
  list(landscape?.market_worries).forEach(item => list(item.evidence_ids).forEach(id => ids.add(id)));
  list(landscape?.global_finance_shift?.evidence_ids).forEach(id => ids.add(id));
  list(landscape?.directional_thesis?.evidence_ids).forEach(id => ids.add(id));
  list(landscape?.what_changed_since_last_cycle).forEach(item => list(item.evidence_ids).forEach(id => ids.add(id)));
  return Array.from(ids);
}

function brief() {
  if (!landscapeRenderable) return `<section id="brief" class="panel"><div class="section-head"><div><p class="eyebrow">Brief</p><h2>Market Landscape Analytic</h2></div><a class="button" href="outputs/market-landscape-state.json">Open artifact</a></div><div class="degraded"><span class="pill bad">Brief blocked</span><p class="judgment">Evidence-backed landscape fields are incomplete or missing. The homepage is refusing to render unsupported market interpretation.</p><div class="artifact-list"><div><span>Required artifacts</span><b>market-landscape-state.json · institutional-evidence-map.json</b></div><div><span>Required condition</span><b>Every Brief field must resolve to evidence_ids.</b></div><div><span>Current trust</span><b>${esc(trustText)}</b></div></div></div></section>`;
  const focus = list(landscape.market_focus).slice(0, 4);
  const worries = list(landscape.market_worries).slice(0, Math.max(0, 4 - focus.length));
  const changed = list(landscape.what_changed_since_last_cycle).slice(0, 3);
  return `<section id="brief" class="panel"><div class="section-head"><div><p class="eyebrow">Brief</p><h2>Evidence-backed Market Landscape</h2></div><a class="button" href="outputs/market-landscape-state.json">Open artifact</a></div><p class="judgment">${esc(landscape.directional_thesis.base_case)}</p><div class="landscape-grid">${focus.map(item => `<article class="landscape-card"><span>Market focused on</span><b>${esc(item.theme)}</b><small>${esc(item.summary)}</small></article>`).join('')}${worries.map(item => `<article class="landscape-card"><span>Market worried about</span><b>${esc(item.risk)}</b><small>${esc(item.summary)}</small></article>`).join('')}</div><div class="translation-strip"><article><span>Global finance shift</span><b>${esc(landscape.global_finance_shift.summary)}</b></article><article><span>What changed</span><b>${esc(changed.map(x => x.change).join(' · '))}</b></article></div><div class="trust-strip"><article><span>Regime posture</span><b>${esc(regime.regimePosture)}</b></article><article><span>Rates pressure</span><b>${esc(regime.ratesPressure)}</b></article><article><span>Liquidity</span><b>${esc(regime.liquidityState)}</b></article><article><span>Volatility</span><b>${esc(regime.volatilityState)}</b></article><article><span>Landscape state change</span><b>${esc(landscape.state_change_level)}</b></article></div><details class="evidence-drawer" open><summary>Evidence drawer · source → extracted insight → thesis affected → confidence</summary>${evidenceCards(allLandscapeEvidenceIds())}</details></section>`;
}

function holdings() { return `<section id="holdings-section" class="panel"><div class="section-head"><div><p class="eyebrow">Holdings</p><h2>Decision machines</h2></div><a class="button" href="outputs/portfolio-decision-state.json">Open artifact</a></div><div class="artifact-grid">${portfolio.map(h => `<article class="artifact-card"><span class="pill ${cls(h.decisionPermission)}">${esc(h.decisionPermission)}</span><h3>${esc(h.ticker)}</h3><div class="artifact-list"><div><span>Price / day</span><b>$${fmt(h.price)} · ${fmt(h.dayChangePct)}%</b></div><div><span>Weight</span><b>${fmt(h.portfolioWeightPct)}%</b></div><div><span>Thesis</span><b>${esc(h.thesisStatus)}</b></div><div><span>Rule breaches</span><b>${esc(list(h.ruleBreaches).join('; ') || 'None')}</b></div><div><span>Add zone</span><b>${esc(h.addZone)}</b></div><div><span>Next evidence</span><b>${esc(h.nextEvidenceRequired)}</b></div><div><span>Data freshness</span><b>${esc(h.dataFreshness)} · ${esc(h.sourceConfidence)}</b></div></div></article>`).join('')}</div></section>`; }
function candidateMatchesTheme(candidate, theme) { const hay = `${candidate.macroThesis || ''} ${candidate.agentSelectionRationale || ''} ${candidate.asymmetryReason || ''}`.toLowerCase(); return String(theme || '').toLowerCase().split(/\s+/).filter(w => w.length > 3).some(word => hay.includes(word)); }
function opportunity() {
  const directions = landscapeRenderable ? list(landscape.market_focus).slice(0, 4) : [];
  const used = new Set();
  const groups = directions.map(direction => { const candidates = opportunities.filter(o => candidateMatchesTheme(o, direction.theme)); candidates.forEach(o => used.add(o.ticker)); return { direction, candidates }; }).filter(group => group.candidates.length);
  const remaining = opportunities.filter(o => !used.has(o.ticker));
  if (!groups.length && remaining.length) groups.push({ direction: { theme: 'Current macro direction', summary: landscapeThesis }, candidates: remaining });
  return `<section id="opportunities-section" class="panel"><div class="section-head"><div><p class="eyebrow">Opportunity</p><h2>Underpriced expressions downstream of macro direction</h2></div><a class="button" href="outputs/opportunity-promotion-state.json">Open artifact</a></div><p class="judgment">Opportunity does not define the macro view. The Brief defines macro direction; this section maps ticker expressions, missing evidence, and promotion gates downstream of that direction.</p>${groups.map(group => `<div class="macro-group"><span>Macro direction</span><h3>${esc(group.direction.theme)}</h3><p>${esc(group.direction.summary)}</p><div class="artifact-grid">${group.candidates.map(o => `<article class="artifact-card"><span class="pill ${cls(o.promotionStatus)}">${esc(o.promotionStatus)}</span><h3>${esc(o.ticker)}</h3><p>${esc(o.macroThesis)}</p><div class="artifact-list"><div><span>Asymmetry reason</span><b>${esc(o.asymmetryReason)}</b></div><div><span>Ticker read</span><b>${esc(o.tickerAnalysisRead)}</b></div><div><span>Score</span><b>${fmt(o.opportunityScore, 0)}</b></div><div><span>Missing evidence</span><b>${esc(list(o.missingEvidence).join('; '))}</b></div><div><span>Promotion gate</span><b>${esc(o.valuationZoneRequired)} · ${esc(o.invalidationRequired)}</b></div><div><span>Assigned task</span><b>${esc(o.assignedAgentTask)}</b></div></div></article>`).join('')}</div></div>`).join('') || '<p class="muted">No opportunity candidates are currently promoted.</p>'}</section>`;
}
function marketTape() { const freshness = truth.perTickerFreshness || {}; const confidence = truth.perMetricConfidence || {}; const rows = Object.keys(freshness).map(ticker => `<article><b>${esc(ticker)}</b><small>Freshness: ${esc(freshness[ticker])}<br>Confidence: ${esc(confidence[ticker])}</small></article>`).join(''); return `<section id="market-section" class="panel"><div class="section-head"><div><p class="eyebrow">Market Tape</p><h2>External macro pressure map</h2></div><a class="button" href="outputs/data-truth-state.json">Open data truth</a></div><div class="trust-strip"><article><span>Render safety</span><b>${esc(trustText)}</b></article><article><span>Stale sources</span><b>${esc(list(truth.staleSources).join(', ') || 'None')}</b></article><article><span>Blocked sources</span><b>${esc(list(truth.blockedSources).join(', ') || 'None')}</b></article><article><span>Cadence</span><b>${esc(cadence.cadence || '4-hour operating cycle')}</b></article><article><span>Immediate trigger</span><b>${cadence.shouldRunImmediately ? 'Yes' : 'No'}</b></article></div><div class="source-list">${rows}</div></section>`; }

const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width, initial-scale=1"/><title>OpenClaw Capital Radar</title><link rel="stylesheet" href="assets/capital-radar.css"/>${style}</head><body><main class="shell"><div class="topbar"><div class="brand"><span class="mark">◇</span><div>OpenClaw Capital Radar</div></div><nav class="nav"><a href="#brief">Brief</a><a href="#holdings-section">Holdings</a><a href="#opportunities-section">Opportunity</a><a href="#market-section">Market Tape</a></nav><div id="generated">Artifact render · ${esc(landscape?.as_of || regime.generatedAt || '')}</div></div><header class="hero"><div><p class="eyebrow">Operating brain · validated artifacts</p><h1>Capital Radar</h1><p class="lede">${esc(landscapeThesis || 'Decision artifacts pending.')}</p><div class="lens-strip"><span>Market landscape</span><span>Evidence map</span><span>Rules</span><span>Validation</span></div></div><aside class="status"><span>Homepage trust</span><strong class="${health === 'fresh' ? 'good' : 'bad'}">${esc(trustText)}</strong><span>OpenClaw interprets evidence. Artifacts preserve state. Website renders validated radar.</span><span>Deploy only on meaningful decision-state change.</span></aside></header>${brief()}${holdings()}${opportunity()}${marketTape()}<footer class="footer"><span>Research and education only. No brokerage connection.</span><a class="button" href="outputs/institutional-evidence-map.json">Open evidence map</a><a class="button" href="outputs/operating-cadence-state.json">Open cadence state</a></footer></main></body></html>`;
fs.writeFileSync(indexPath, html);
console.log('rendered evidence-backed operating-brain homepage');
