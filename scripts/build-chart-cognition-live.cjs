const fs = require('fs');
const path = require('path');
const root = path.join(__dirname, '..');
const outDir = path.join(root, 'outputs');
fs.mkdirSync(outDir, { recursive: true });
const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>OpenClaw Chart Cognition</title><style>
:root{--bg:#050505;--ink:#f4f1ea;--muted:#a7a29a;--line:rgba(244,241,234,.15);--panel:rgba(244,241,234,.06);--panel2:rgba(244,241,234,.1);--good:#62d78e;--warn:#d9a84d;--bad:#ff756d;--blue:#92c7ff}*{box-sizing:border-box}body{margin:0;background:#050505;color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Arial,sans-serif}body:before{content:"";position:fixed;inset:0;pointer-events:none;background:radial-gradient(circle at 80% 5%,rgba(216,168,74,.18),transparent 28%),radial-gradient(circle at 8% 25%,rgba(146,199,255,.10),transparent 24%),linear-gradient(180deg,#0c0c0b 0%,#050505 42%,#080806 100%);z-index:-1}.page{max-width:1680px;margin:auto;padding:18px clamp(14px,3vw,48px) 80px}.top{position:sticky;top:0;z-index:20;display:flex;justify-content:space-between;gap:12px;align-items:center;padding:14px 0;background:rgba(5,5,5,.78);backdrop-filter:blur(20px);border-bottom:1px solid var(--line)}a{color:var(--ink)}.brand{font-weight:760;letter-spacing:-.03em}.nav{display:flex;gap:8px;flex-wrap:wrap}.pill,.nav a,button{border:1px solid var(--line);border-radius:999px;background:rgba(244,241,234,.055);color:var(--ink);padding:9px 12px;text-decoration:none;font-size:12px;cursor:pointer}.active{background:var(--ink)!important;color:#050505!important}.hero{padding:38px 0 24px;border-bottom:1px solid var(--line)}.kicker{color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.16em;font-weight:850}.hero h1{font-size:clamp(44px,8vw,112px);line-height:.88;letter-spacing:-.075em;margin:12px 0 18px;font-weight:560}.lede{max-width:980px;color:#d7d2ca;font-size:clamp(16px,1.7vw,23px);line-height:1.42}.grid{display:grid;grid-template-columns:minmax(0,1fr) 400px;gap:16px;padding-top:18px}.panel{border:1px solid var(--line);border-radius:28px;background:linear-gradient(180deg,var(--panel2),var(--panel));padding:16px}.toolbar{display:flex;flex-wrap:wrap;gap:8px;align-items:center;justify-content:space-between;margin-bottom:12px}.group{display:flex;gap:7px;flex-wrap:wrap}.chartbox{height:600px;border:1px solid var(--line);border-radius:22px;background:#020202;padding:8px;overflow:hidden}.subgrid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;margin-top:10px}.subchart{height:150px;border:1px solid var(--line);border-radius:18px;background:#020202;padding:6px}.label{display:block;color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.13em;font-weight:850}.metricgrid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.metric{border:1px solid rgba(244,241,234,.12);border-radius:18px;padding:12px;background:rgba(0,0,0,.18)}.metric b{display:block;font-size:22px;margin-top:5px;letter-spacing:-.04em}.decision h2{font-size:28px;letter-spacing:-.04em;margin:8px 0}.decision p{color:#d6d0c8;line-height:1.45}.zones{display:grid;grid-template-columns:1fr 1fr;gap:8px}.zones label{display:grid;gap:4px}.zones input,.manual input{width:100%;background:#020202;border:1px solid rgba(244,241,234,.18);border-radius:12px;color:var(--ink);padding:9px}.manual{display:grid;grid-template-columns:1fr 90px;gap:8px}.small{font-size:12px;color:var(--muted);line-height:1.4}pre{white-space:pre-wrap;overflow:auto;background:#020202;border:1px solid rgba(244,241,234,.13);border-radius:18px;padding:12px;color:#d6d0c8;font-size:11px}.legend{display:flex;gap:10px;flex-wrap:wrap;color:var(--muted);font-size:11px}.dot{display:inline-block;width:9px;height:9px;border-radius:99px;margin-right:5px}.good{color:var(--good)}.warn{color:var(--warn)}.bad{color:var(--bad)}@media(max-width:1100px){.grid{grid-template-columns:1fr}.subgrid{grid-template-columns:1fr}.chartbox{height:480px}.nav{display:none}}@media(max-width:620px){.metricgrid,.zones{grid-template-columns:1fr}.page{padding-inline:12px}}
</style></head><body><main class="page"><div class="top"><div class="brand">OpenClaw · Chart Cognition</div><nav class="nav"><a href="/">Dashboard</a><a href="/outputs/capital-radar-current.html">Capital Radar</a><a href="#chart">Chart</a><a href="#data">Data</a></nav></div><header class="hero"><span class="kicker">Live ticker workbench · chart as decision map</span><h1>Make price structure actionable.</h1><p class="lede">This page loads generated OHLCV candle files for every holding/watch ticker, then overlays indicators, range controls, manual levels, and OpenClaw reaction zones. If live candle files are unavailable, it falls back to mock data and marks that state visibly.</p></header><section id="chart" class="grid"><div class="panel"><div class="toolbar"><div class="group" id="symbols"></div><div class="group" id="ranges"></div><div class="group" id="intervals"></div></div><div class="toolbar"><div class="group" id="toggles"></div><div class="legend"><span><i class="dot" style="background:#155e75"></i>buy</span><span><i class="dot" style="background:#064e3b"></i>fear discount</span><span><i class="dot" style="background:#713f12"></i>premium</span><span><i class="dot" style="background:#92c7ff"></i>SMA20</span><span><i class="dot" style="background:#10b981"></i>VWAP</span></div></div><div class="chartbox"><svg id="priceChart" viewBox="0 0 1120 600" width="100%" height="100%"></svg></div><div class="chartbox" style="height:140px;margin-top:10px"><svg id="volumeChart" viewBox="0 0 1120 140" width="100%" height="100%"></svg></div><div class="subgrid"><div class="subchart"><svg id="rsiChart" viewBox="0 0 360 150" width="100%" height="100%"></svg></div><div class="subchart"><svg id="macdChart" viewBox="0 0 360 150" width="100%" height="100%"></svg></div><div class="subchart"><svg id="atrChart" viewBox="0 0 360 150" width="100%" height="100%"></svg></div></div></div><aside class="panel"><div class="decision"><span class="label">Decision</span><h2 id="state">Loading</h2><p id="action">Loading chart data…</p></div><div class="metricgrid"><div class="metric"><span class="label">Technical</span><b id="techScore">--</b></div><div class="metric"><span class="label">Exp. gap</span><b id="gapScore">--</b></div><div class="metric"><span class="label">Regime</span><b id="regime">--</b></div></div><hr style="border-color:rgba(244,241,234,.14);margin:16px 0"><span class="label">Editable reaction + valuation zones</span><div class="zones" id="zoneInputs"></div><hr style="border-color:rgba(244,241,234,.14);margin:16px 0"><span class="label">Manual watch levels</span><div class="manual"><input id="manualLabel" placeholder="Label"><input id="manualPrice" placeholder="Price"></div><button style="width:100%;margin-top:8px" id="addManual">Add level</button><div id="manualList" class="small" style="margin-top:10px"></div><hr style="border-color:rgba(244,241,234,.14);margin:16px 0"><span class="label">Reading</span><p class="small">Long ranges define regime. Short ranges define execution. Zones define allowed behavior. Indicators confirm or weaken the setup. Research only; no brokerage action.</p><section id="data"><span class="label">Data status</span><pre id="contractText"></pre></section></aside></section></main><script>
const seeds={GEV:900,NVDA:180,CEG:270,AMZN:220,META:640,SPY:670,MSFT:420,MA:520,NFLX:950,BMNR:24,CONL:8.5,TSLT:23,TSNF:10,QQQ:600,IWM:240,'^VIX':18,'BTC-USD':100000,'ETH-USD':3200,AVGO:360,VRT:150,GOOGL:180,IBIT:60};
const rangeDays={"1M":22,"3M":66,"6M":132,"1Y":252,"2Y":504,MAX:1250};
const intervalStep={"1D":1,"1W":5,"1M":21};
const state={symbol:'SPY',range:'1Y',interval:'1D',dataStatus:'mock',toggles:{zones:true,fairValue:true,sma20:true,sma50:true,sma100:false,sma200:true,ema21:false,bb:true,vwap:true,events:true,custom:true},zones:{buyLow:900,buyHigh:950,addLow:800,addHigh:850,trimLow:1150,trimHigh:1250,stopReview:875,hardExit:820,target:1200,fairValueLow:960,fairValueHigh:1080},manual:[{label:'Manual watch',price:1010}],all:[]};
function safeName(symbol){return String(symbol).replace(/[^A-Za-z0-9._-]/g,'_').toUpperCase()}
async function fetchJsonSafe(url){try{const r=await fetch(url,{cache:'no-store'});if(!r.ok)return null;return await r.json()}catch(e){return null}}
function gen(seed=900,count=720){const rows=[];let prev=seed*.62;const start=new Date('2023-07-03T00:00:00Z');for(let i=0;rows.length<count;i++){const d=new Date(start);d.setDate(start.getDate()+i);if(d.getDay()==0||d.getDay()==6)continue;const trend=i*(seed/720)*.48,cycleA=Math.sin(i/18)*seed*.035,cycleB=Math.sin(i/71)*seed*.08,shock=i>320&&i<365?-(i-320)*seed*.0013:i>=365&&i<430?-seed*.058+(i-365)*seed*.0011:0,euph=i>560?(i-560)*seed*.00095:0;const close=Math.max(8,seed*.62+trend+cycleA+cycleB+shock+euph+Math.sin(i*1.91)*seed*.012);const open=prev,high=Math.max(open,close)+seed*.006+Math.abs(Math.sin(i*.73))*seed*.018,low=Math.min(open,close)-seed*.006-Math.abs(Math.cos(i*.53))*seed*.018,volume=Math.round(7e5+Math.abs(close-open)*52000+Math.abs(Math.sin(i/9))*1600000+(i>560?5e5:0));rows.push({time:d.toISOString().slice(0,10),label:d.toISOString().slice(5,10),open:+open.toFixed(2),high:+high.toFixed(2),low:+low.toFixed(2),close:+close.toFixed(2),volume});prev=close}return rows}
function compress(rows,step){if(step===1)return rows;const out=[];for(let i=0;i<rows.length;i+=step){const g=rows.slice(i,i+step);if(!g.length)continue;out.push({time:g[g.length-1].time,label:g[g.length-1].label,open:g[0].open,high:Math.max(...g.map(x=>x.high)),low:Math.min(...g.map(x=>x.low)),close:g[g.length-1].close,volume:g.reduce((s,x)=>s+x.volume,0)})}return out}
function candles(){return compress(state.all.slice(-rangeDays[state.range]),intervalStep[state.interval])}
function sma(c,p){const o=[];for(let i=p-1;i<c.length;i++)o.push({i,value:c.slice(i-p+1,i+1).reduce((s,x)=>s+x.close,0)/p});return o}
function ema(c,p){const k=2/(p+1);let prev=c[0]?.close||0;return c.map((x,i)=>{const v=i?x.close*k+prev*(1-k):x.close;prev=v;return{i,value:v}})}
function vwap(c){let pv=0,vol=0;return c.map((x,i)=>{const t=(x.high+x.low+x.close)/3;pv+=t*x.volume;vol+=x.volume;return{i,value:pv/Math.max(vol,1)}})}
function bb(c,p=20,m=2){const mid=sma(c,p),up=[],lo=[];mid.forEach(x=>{const sl=c.slice(x.i-p+1,x.i+1),sd=Math.sqrt(sl.reduce((s,y)=>s+(y.close-x.value)**2,0)/p);up.push({i:x.i,value:x.value+m*sd});lo.push({i:x.i,value:x.value-m*sd})});return{mid,up,lo}}
function rsi(c,p=14){const out=[];let ag=0,al=0;for(let i=1;i<c.length;i++){const d=c[i].close-c[i-1].close,g=Math.max(d,0),l=Math.max(-d,0);if(i<=p){ag+=g;al+=l;if(i<p)continue;ag/=p;al/=p}else{ag=(ag*(p-1)+g)/p;al=(al*(p-1)+l)/p}const rs=ag/Math.max(al,.0001);out.push({i,value:100-100/(1+rs)})}return out}
function macd(c){const f=ema(c,12),s=ema(c,26),line=f.map((x,i)=>({i:x.i,value:x.value-(s[i]?.value||x.value)}));const sig=ema(line.map(x=>({close:x.value})),9).map((x,i)=>({i:line[i]?.i||i,value:x.value}));return line.map((x,i)=>({i:x.i,value:x.value-(sig[i]?.value||0)}))}
function atr(c,p=14){const tr=c.map((x,i)=>{const prev=c[i-1]?.close||x.close;return Math.max(x.high-x.low,Math.abs(x.high-prev),Math.abs(x.low-prev))});const out=[];for(let i=p-1;i<c.length;i++)out.push({i,value:tr.slice(i-p+1,i+1).reduce((s,x)=>s+x,0)/p});return out}
function path(points){return points.map((p,i)=>(i?'L':'M')+p.x.toFixed(1)+','+p.y.toFixed(1)).join(' ')}
function clear(svg){while(svg.firstChild)svg.removeChild(svg.firstChild)}
function el(name,attrs){const e=document.createElementNS('http://www.w3.org/2000/svg',name);for(const k in attrs)e.setAttribute(k,attrs[k]);return e}
function txt(svg,x,y,t,fill='#a7a29a',size=11,anchor='start'){const e=el('text',{x,y,fill,'font-size':size,'text-anchor':anchor});e.textContent=t;svg.appendChild(e)}
function drawPrice(c){const svg=document.getElementById('priceChart');clear(svg);const W=1120,H=600,p={t:28,r:88,b:48,l:48},pw=W-p.l-p.r,ph=H-p.t-p.b,z=state.zones;const vals=[...c.flatMap(x=>[x.high,x.low]),z.trimHigh,z.target,z.addLow,z.hardExit,z.fairValueLow,z.fairValueHigh];const max=Math.max(...vals)*1.015,min=Math.min(...vals)*.985;const X=i=>p.l+(i/Math.max(1,c.length-1))*pw,Y=v=>p.t+((max-v)/(max-min))*ph;svg.appendChild(el('rect',{width:W,height:H,fill:'#050505'}));for(let i=0;i<7;i++){const v=min+(max-min)*i/6;svg.appendChild(el('line',{x1:p.l,x2:p.l+pw,y1:Y(v),y2:Y(v),stroke:'#1f1f1f'}));txt(svg,p.l+pw+8,Y(v)+4,'$'+v.toFixed(v<20?2:0))}function zone(lo,hi,label,fill,op=.14){if(!state.toggles.zones&&!/FAIR/.test(label))return;const top=Y(hi),bot=Y(lo);svg.appendChild(el('rect',{x:p.l,y:top,width:pw,height:Math.max(1,bot-top),fill,opacity:op}));txt(svg,p.l+10,top+18,label)}if(state.toggles.fairValue)zone(z.fairValueLow,z.fairValueHigh,'AI FAIR VALUE','#3f3f46',.22);zone(z.addLow,z.addHigh,'FEAR DISCOUNT / DEEP ADD','#064e3b');zone(z.buyLow,z.buyHigh,'BUY / STARTER','#155e75');zone(z.trimLow,z.trimHigh,'EXPECTATION PREMIUM / TRIM','#713f12');function pl(v,label,color,dash='6 6'){if(!state.toggles.zones)return;svg.appendChild(el('line',{x1:p.l,x2:p.l+pw,y1:Y(v),y2:Y(v),stroke:color,'stroke-dasharray':dash,'stroke-width':1.4}));txt(svg,p.l+pw+8,Y(v)+4,label,color)}pl(z.stopReview,'STOP','#fb923c');pl(z.hardExit,'EXIT','#ef4444');pl(z.target,'TARGET','#eab308','3 5');const cw=Math.max(1.4,Math.min(8,pw/c.length*.58));c.forEach((x,i)=>{const up=x.close>=x.open,cx=X(i),oy=Y(x.open),cy=Y(x.close),hy=Y(x.high),ly=Y(x.low),by=Math.min(oy,cy),bh=Math.max(1.7,Math.abs(cy-oy));svg.appendChild(el('line',{x1:cx,x2:cx,y1:hy,y2:ly,stroke:up?'#e7e5e4':'#737373','stroke-width':1}));svg.appendChild(el('rect',{x:cx-cw/2,y:by,width:cw,height:bh,rx:1,fill:up?'#d6d3d1':'#525252'}))});const line=(series,color,w=2,dash='')=>{if(series.length<2)return;svg.appendChild(el('path',{d:path(series.map(s=>({x:X(s.i),y:Y(s.value)}))),fill:'none',stroke:color,'stroke-width':w,'stroke-dasharray':dash}))};const b=bb(c);if(state.toggles.bb){line(b.up,'#525252',1.2,'3 4');line(b.lo,'#525252',1.2,'3 4')}if(state.toggles.sma20)line(sma(c,20),'#38bdf8');if(state.toggles.sma50)line(sma(c,50),'#a78bfa');if(state.toggles.sma100)line(sma(c,100),'#f59e0b',1.6);if(state.toggles.sma200)line(sma(c,200),'#ef4444',1.6);if(state.toggles.ema21)line(ema(c,21),'#f5f5f4',1.5,'4 4');if(state.toggles.vwap)line(vwap(c),'#10b981',1.8,'2 3');if(state.toggles.events){[{i:.18,l:'Earnings'},{i:.39,l:'Fed/CPI'},{i:.62,l:'News shock'},{i:.82,l:'Analyst rev.'}].forEach(m=>{const ix=Math.floor(c.length*m.i),cx=X(ix);svg.appendChild(el('line',{x1:cx,x2:cx,y1:p.t,y2:p.t+ph,stroke:'#404040','stroke-dasharray':'2 6'}));svg.appendChild(el('circle',{cx,cy:p.t+12,r:4,fill:'#d4d4d4'}));txt(svg,cx+7,p.t+16,m.l)})}if(state.toggles.custom)state.manual.forEach(m=>{svg.appendChild(el('line',{x1:p.l,x2:p.l+pw,y1:Y(m.price),y2:Y(m.price),stroke:'#22c55e','stroke-dasharray':'2 4'}));txt(svg,p.l+pw+8,Y(m.price)+4,m.label,'#22c55e')});const last=c[c.length-1];svg.appendChild(el('circle',{cx:X(c.length-1),cy:Y(last.close),r:5,fill:'#f5f5f4'}));txt(svg,X(c.length-1)-72,Y(last.close)-10,'LAST $'+last.close.toFixed(last.close<20?2:0),'#f5f5f4');for(let i=0;i<c.length;i+=Math.max(1,Math.floor(c.length/9)))txt(svg,X(i),H-18,c[i].label,'#737373',10,'middle')}
function drawBars(id,c,series,label,lines=[]){const svg=document.getElementById(id);clear(svg);const W=+svg.viewBox.baseVal.width,H=+svg.viewBox.baseVal.height,p={t:18,r:28,b:22,l:38};svg.appendChild(el('rect',{width:W,height:H,fill:'#050505'}));txt(svg,p.l,14,label,'#737373',11);const vals=series.length?series.map(x=>x.value):[0],min=Math.min(...vals,...lines),max=Math.max(...vals,...lines);const X=i=>p.l+(i/Math.max(1,c.length-1))*(W-p.l-p.r),Y=v=>p.t+((max-v)/Math.max(.001,max-min))*(H-p.t-p.b);lines.forEach(l=>svg.appendChild(el('line',{x1:p.l,x2:W-p.r,y1:Y(l),y2:Y(l),stroke:'#333','stroke-dasharray':'5 5'})));if(series.length>1)svg.appendChild(el('path',{d:path(series.map(s=>({x:X(s.i),y:Y(s.value)}))),fill:'none',stroke:'#d6d3d1','stroke-width':2}))}
function drawVolume(c){const svg=document.getElementById('volumeChart');clear(svg);const W=1120,H=140,p={t:16,r:20,b:22,l:48},max=Math.max(...c.map(x=>x.volume),1),bw=(W-p.l-p.r)/c.length;svg.appendChild(el('rect',{width:W,height:H,fill:'#050505'}));txt(svg,p.l,14,'VOLUME / PARTICIPATION','#737373',11);c.forEach((x,i)=>{const h=(H-p.t-p.b)*x.volume/max;svg.appendChild(el('rect',{x:p.l+i*bw,y:H-p.b-h,width:Math.max(1,bw-1),height:h,fill:x.close>=x.open?'#57534e':'#404040'}))})}
function decide(c){const last=c[c.length-1],p=last.close,z=state.zones;if(p<=z.hardExit)return['HARD EXIT','Exit or materially reduce. Structure failed.','bad',88];if(p<=z.stopReview)return['STOP REVIEW','Freeze new capital. Confirm panic discount vs thesis damage.','warn',76];if(p>=z.trimLow)return['TRIM INTO STRENGTH','Reduce in layers or tighten stop. Expectation premium is elevated.','warn',72];if(p>=z.buyLow&&p<=z.buyHigh)return['BUY ZONE','Starter allowed only with thesis intact and stabilization evidence.','good',70];if(p>=z.addLow&&p<=z.addHigh)return['DEEP ADD ZONE','Add only if selling is liquidity-driven, not fundamental damage.','good',64];if(p>=z.fairValueLow&&p<=z.fairValueHigh)return['FAIR VALUE','No edge. Let thesis mature or wait for reaction zone.','',66];return['WAIT / HOLD','No chase. Wait for reaction zone, consolidation, or confirmed breakout.','',61]}
function render(){const c=candles();if(c.length<5)return;drawPrice(c);drawVolume(c);drawBars('rsiChart',c,rsi(c),'RSI 14',[30,50,70]);drawBars('macdChart',c,macd(c),'MACD HISTOGRAM',[0]);drawBars('atrChart',c,atr(c),'ATR / VOLATILITY',[]);const d=decide(c),last=c[c.length-1],s20=sma(c,20).at(-1)?.value||last.close,s50=sma(c,50).at(-1)?.value||last.close,rv=rsi(c).at(-1)?.value||50;let tech=50;if(last.close>s20)tech+=12;if(s20>s50)tech+=10;if(rv>70)tech-=12;if(rv<35)tech+=8;if(last.close<state.zones.stopReview)tech-=25;if(last.close>state.zones.trimLow)tech-=10;tech=Math.max(0,Math.min(100,Math.round(tech)));const mid=(state.zones.fairValueLow+state.zones.fairValueHigh)/2,gap=(last.close-mid)/mid*100;document.getElementById('state').textContent=d[0];document.getElementById('action').textContent=d[1];document.getElementById('techScore').textContent=tech;document.getElementById('gapScore').textContent=gap.toFixed(1)+'%';document.getElementById('regime').textContent=tech>65?'Trend':tech<40?'Risk':'Mixed';document.getElementById('contractText').textContent='symbol: '+state.symbol+'\ndata: '+state.dataStatus+'\ncandles: '+state.all.length+'\nvisible candles: '+c.length+'\nfile: /data/market-candles/'+safeName(state.symbol)+'.json\nURL: /outputs/chart-cognition.html?symbol='+state.symbol;renderManual()}
function btns(id,items,key){const box=document.getElementById(id);box.innerHTML='';items.forEach(v=>{const b=document.createElement('button');b.textContent=v;b.className=state[key]===v?'active':'';b.onclick=async()=>{state[key]=v;if(key==='symbol'){history.replaceState(null,'','?symbol='+encodeURIComponent(v));await loadSymbolLive(v);return}renderControls();render()};box.appendChild(b)})}
function renderControls(){btns('symbols',Object.keys(seeds),'symbol');btns('ranges',Object.keys(rangeDays),'range');btns('intervals',Object.keys(intervalStep),'interval');const tg=document.getElementById('toggles');tg.innerHTML='';Object.keys(state.toggles).forEach(k=>{const b=document.createElement('button');b.textContent=k.toUpperCase();b.className=state.toggles[k]?'active':'';b.onclick=()=>{state.toggles[k]=!state.toggles[k];renderControls();render()};tg.appendChild(b)});const zi=document.getElementById('zoneInputs');zi.innerHTML='';Object.keys(state.zones).forEach(k=>{const l=document.createElement('label');l.innerHTML='<span class="label">'+k+'</span><input value="'+state.zones[k]+'">';l.querySelector('input').onchange=e=>{const n=Number(e.target.value);if(Number.isFinite(n)){state.zones[k]=n;render()}};zi.appendChild(l)})}
function renderManual(){const list=document.getElementById('manualList');list.innerHTML=state.manual.map(m=>'<div style="display:flex;justify-content:space-between;border-top:1px solid rgba(244,241,234,.12);padding:7px 0"><span>'+m.label+'</span><b>$'+m.price+'</b></div>').join('')}
function loadMock(){state.all=gen(seeds[state.symbol]||100);state.dataStatus='mock fallback'}
async function loadManifest(){const params=new URLSearchParams(location.search);const q=params.get('symbol');const m=await fetchJsonSafe('/data/market-candles/manifest.json');if(m&&Array.isArray(m.symbols)){m.symbols.forEach(s=>{if(!seeds[s])seeds[s]=100});state.dataStatus='manifest loaded'}if(q){state.symbol=q.toUpperCase();if(!seeds[state.symbol])seeds[state.symbol]=100}renderControls();await loadSymbolLive(state.symbol)}
async function loadSymbolLive(symbol){const payload=await fetchJsonSafe('/data/market-candles/'+safeName(symbol)+'.json');if(payload&&Array.isArray(payload.candles)&&payload.candles.length>30){state.all=payload.candles.map(x=>({time:String(x.time),label:String(x.time).slice(5,10),open:+x.open,high:+x.high,low:+x.low,close:+x.close,volume:+(x.volume||0)})).filter(x=>Number.isFinite(x.close));state.dataStatus='live static candles · '+(payload.asOf||'as-of unknown');renderControls();render();return}loadMock();renderControls();render()}
document.getElementById('addManual').onclick=()=>{const label=document.getElementById('manualLabel').value.trim(),price=Number(document.getElementById('manualPrice').value);if(label&&Number.isFinite(price)){state.manual.push({label,price});document.getElementById('manualLabel').value='';document.getElementById('manualPrice').value='';render()}};
loadManifest();setInterval(()=>loadSymbolLive(state.symbol),60000);
</script></body></html>`;
fs.writeFileSync(path.join(outDir, 'chart-cognition.html'), html);
console.log('built live-aware chart cognition page');
