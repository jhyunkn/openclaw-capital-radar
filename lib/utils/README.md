# Utility Helpers

General helpers belong here.

Examples:

- number formatting
- date formatting
- small data normalization helpers
- confidence label helpers

Rule: utilities should stay generic. Market-specific scoring belongs in `lib/scoring`.
